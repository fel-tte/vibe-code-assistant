[pytest]
pythonpath = .

markers =
    integration: marks tests as integration tests requiring a database session
    real_provider: marks tests that call real provider APIs (skipped without credentials)
    load: marks tests as load/performance tests
