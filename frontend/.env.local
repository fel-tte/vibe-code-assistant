# Public API base URL for client-side calls (used by browser, must be accessible from user machine)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Server-side base URL for Next.js API routes proxying (used server-side in Docker; api:8000 reachable from frontend container)
API_BASE_URL=http://api:8000/api/v1
